const strandsAgents = require('../index');

describe('strands-agents', () => {
  test('hello function returns the correct greeting', () => {
    expect(strandsAgents.hello()).toBe('Hello from strands-agents!');
  });
});
